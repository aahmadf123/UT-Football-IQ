{
  "name": "football-iq-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers edge API for Football-IQ",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260724.1",
    "@typescript-eslint/eslint-plugin": "^8.61.0",
    "@typescript-eslint/parser": "^8.63.0",
    "eslint": "^10.7.0",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "wrangler": "^4.110.0"
  }
}
